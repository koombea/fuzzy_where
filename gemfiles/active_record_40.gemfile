source 'https://rubygems.org'

gem 'activerecord', '~> 4.0.0', :require => 'active_record'

platforms :ruby do
  if RUBY_VERSION > "2.1.0"
    gem 'sqlite3'
    gem 'test-unit'
  else
    gem 'sqlite3', '1.3.8'
  end
end

gemspec :path => '../'
